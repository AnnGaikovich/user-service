<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <springProperty scope="context" name="appName" source="spring.application.name"/>
    <springProperty scope="context" name="profile" source="spring.profiles.active" defaultValue="local"/>

    <!-- Loki Appender -->
    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>http://localhost:3100/loki/api/v1/push</url>
            <connectionTimeout>5000</connectionTimeout>
            <requestTimeout>10000</requestTimeout>
        </http>
        <format>
            <label>
                <pattern>app=${appName},level=%level,env=${profile}</pattern>
            </label>
            <message>
                <pattern>{
                    "timestamp": "%date{ISO8601}",
                    "thread": "%thread",
                    "level": "%level",
                    "logger": "%logger{36}",
                    "service": "${appName}",
                    "traceId": "%X{traceId:-}",
                    "spanId": "%X{spanId:-}",
                    "message": "%msg",
                    "exception": "%ex{full}"
                    }</pattern>
            </message>
            <timestamp>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestamp>
        </format>
        <batchTimeout>5</batchTimeout>
        <batchSize>102400</batchSize>
    </appender>

    <!-- Console Appender with colors -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %highlight(%-5level) [%cyan(${appName})] [%magenta(traceId=%X{traceId:-})] [%yellow(spanId=%X{spanId:-})] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Specific package logging for your service -->
    <logger name="org.example.userservice" level="DEBUG" additivity="false">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Spring Framework logging -->
    <logger name="org.springframework" level="INFO"/>

    <!-- Hibernate SQL logging -->
    <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" additivity="false">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Web logging -->
    <logger name="org.springframework.web" level="DEBUG" additivity="false">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Database connection pool logging -->
    <logger name="com.zaxxer.hikari" level="INFO"/>

    <!-- Cache logging -->
    <logger name="org.springframework.cache" level="DEBUG" additivity="false">
        <appender-ref ref="LOKI"/>
        <appender-ref ref="CONSOLE"/>
    </logger>
</configuration>